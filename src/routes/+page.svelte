<script lang="ts">
  /**
   * Route entry point.
   *
   * Everything lives in components under `$lib`; this file only mounts the
   * application shell so the route layer stays a routing concern.
   */
  import AppShell from "$lib/components/AppShell.svelte";
</script>

<AppShell />
