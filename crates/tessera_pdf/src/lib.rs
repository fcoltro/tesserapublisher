//! PDF/X generation. Depends on the document, never on the renderer.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() {
        // Replaced by real tests as each task lands.
        assert_eq!(2 + 2, 4);
    }
}
