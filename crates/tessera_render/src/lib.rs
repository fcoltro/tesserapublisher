//! Builds a vello::Scene from a resolved document, and rasterizes it.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() {
        // Replaced by real tests as each task lands.
        assert_eq!(2 + 2, 4);
    }
}
