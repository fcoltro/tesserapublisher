//! The document model, undo/redo, and the .tessera file format.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() {
        // Replaced by real tests as each task lands.
        assert_eq!(2 + 2, 4);
    }
}
