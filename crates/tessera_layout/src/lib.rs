//! Resolves a document into drawable items, shared by the renderer and the PDF writer.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() {
        // Replaced by real tests as each task lands.
        assert_eq!(2 + 2, 4);
    }
}
