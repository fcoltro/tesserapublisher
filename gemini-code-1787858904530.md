# Phase 1: Core Foundation & Rendering

## 1. Workspace Initialization
- [x] Initialize Tauri v2 project with Svelte 5 (Runes) and Vite.
- [x] Configure `Cargo.toml` with workspaces (e.g., `core`, `renderer`, `app`).
- [x] Set up basic IPC communication bridge for testing strings/JSON.

## 2. Document State Architecture (Rust)
- [x] Integrate `bevy_ecs` or `slotmap` for document state.
- [x] Define core entities: `Document`, `Page`, `Layer`, `Frame`.
- [x] Define core components: `Transform` (Position/Rotation/Scale), `ZIndex`, `BoundingBox`.
- [x] Implement a thread-safe state wrapper (e.g., `RwLock<World>`) inside Tauri's managed state.
- [x] Implement an immutable Undo/Redo stack using the `im` crate (tracking state deltas).

## 3. The Rendering Engine (WebGPU + Vello)
- [x] Mount a full-screen `<canvas>` in Svelte and capture its WebGPU context.
- [x] Initialize the `vello` renderer in Rust, binding it to the canvas surface.
- [x] Build the scene generator: Iterate through ECS entities and compile them into a `vello::Scene`.
- [x] Implement the render loop (triggering redraws only when ECS state changes).

## 4. Camera & Workspace Interaction
- [ ] Implement a `Camera` component (Pan X/Y, Zoom Level).
- [ ] Write affine transformation matrix logic to map screen coordinates to document coordinates.
- [ ] Bind frontend mouse wheel/trackpad events to IPC commands to update the camera.
- [ ] Implement basic selection logic (Raycasting from mouse click to ECS bounding boxes).